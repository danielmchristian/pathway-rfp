# Local dev — talk to a host-mode backend on :8000.
NEXT_PUBLIC_API_BASE_URL=http://localhost:8000

# Docker Compose — uncomment the line below and comment the one above.
# NEXT_PUBLIC_API_BASE_URL=http://backend:8000
